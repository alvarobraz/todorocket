import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: 25,
    height: 19,
    borderRadius: 12,
    backgroundColor: '#333333'
  },
  textCounter: {
    textAlign: 'center',
    fontSize: 10,
    color: '#FFFFFF'
  },
})